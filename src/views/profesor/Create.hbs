<section id="main-container">
	<div class="container">
		<div clas="row">
			<div class="col s10 push-s1">
				<div class="row">
					
					<div class="col s12 m7">
						<div class="row">
							<div class="signup-box">
								<div id="quizz" class="quizz">
                <h3>Creación de cuestionario</h3>
                <h5>Selecciona los grupos</h5> 
      <ul class="collection">
        {{#each grupos}}
    <li class="collection-item avatar">
      
      <span class="title">GRUPO: {{nom_gru}}</span>
     
      <a id="check{{id_gru}}" onclick="añadir2({{id_gru}})" class="secondary-content"><i class="material-icons" id="icongru{{id_gru}}">check_box_outline_blank
</i></a>
    </li>
    {{/each}}
  </ul>            
                <h5>Selecciona las preguntas</h5> 
    <ul class="collection">
        {{#each preguntas}}
    <li class="collection-item avatar">
      
      <span class="title">{{con_pre}}</span>
      <p>a){{res_cor}} <br>
         b)Second <br>
         c)third<br>
         d)for
      </p>
      <a id="check{{id_bpr}}" onclick="añadir({{id_bpr}})" class="secondary-content"><i class="material-icons" id="icon{{id_bpr}}">check_box_outline_blank
</i></a>
    </li>
    {{/each}}
  </ul>  
                       
            
             <!-- Seleccione una pregunta: 
                <div class="input-field col s12" id="selects">
                <select name="dificultad" id="dificultad">
                <option value="" disabled selected>Escoge una opción</option>
                {{#each preguntas}}
                <option value={{id_bpr}}>
                            {{con_pre}}                   
                {{/each}}
                </select>
                <label>Dificultad</label>
                <a class="btn-floating btn-large waves-effect waves-light red"><i class="material-icons">add</i></a>
                </div> -->
            <form name="preguntas" id="preguntas" method="POST" action="/AddQuizz">
                   Escriba un nombre para el cuestionario
                  <input type="text" id="nombre" name="nombre">
                   <button class="btn waves-effect waves-light" type="submit" name="action" >Agregar<i class="material-icons right">send</i></button>
            </form>

								
                                                                <p id="avisos"></p>
							</div>
						</div>
					</div>
				</div>
			</div>
		</div>
	</div>

</section>
        
            
<script>
    
    document.addEventListener('DOMContentLoaded', function() {
    var elems = document.querySelectorAll('select');
    var instances = M.FormSelect.init(elems, options);
  });
</script>

{{#each 2}}
<script>
</script>
{{/each}}
<script>
    function añadir(id){
        let att=document.getElementById('icon'+id);
        let form=document.getElementById('preguntas'); 
        if(att.innerText!="check_box"){
          att.innerText="check_box";
          att.value=true;
          form.innerHTML+="<input id='element"+id+"' type='hidden' value='"+id+"' readonly name='element'></input>";
        }else{
          att.innerText="check_box_outline_blank";
          document.getElementById('element'+id).remove(); 

        }
    }
    function añadir2(id){
        let att=document.getElementById('icongru'+id);
        let form=document.getElementById('preguntas'); 
        if(att.innerText!="check_box"){
          att.innerText="check_box";
          att.value=true;
          form.innerHTML+="<input id='group' type='hidden' value='"+id+"' readonly name='group'></input>";
        }else{
          att.innerText="check_box_outline_blank";
          document.getElementById('element'+id).remove(); 

        }
    }
    
</script>
{{res_cor}}<br>
{{nom_tem}}<br>
{{cat_dif}}<br>